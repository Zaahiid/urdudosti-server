import { Router } from "express";

const router = Router();

router.post("/", (req, res) => {
    try {

        
        
    } catch (error) {
        console.log(error);
    }
})

export default router